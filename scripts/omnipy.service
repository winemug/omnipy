[Unit]
Description=Omnipy Rest API
After=network.target

[Service]
ExecStart=/usr/bin/python3 -u $HOME/omnipy/restapi.py
WorkingDirectory=$HOME/omnipy
StandardOutput=inherit
StandardError=inherit
Restart=always
User=pi

[Install]
WantedBy=multi-user.target
